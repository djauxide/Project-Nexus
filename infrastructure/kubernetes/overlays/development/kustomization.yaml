apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nexus-broadcast

resources:
  - ../../base

replicas:
  - name: nexus-api
    count: 1

configMapGenerator:
  - name: nexus-config
    behavior: merge
    literals:
      - NODE_ENV=development
      - LOG_LEVEL=debug
